# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/

# Runtime data (user progress, audio, caches, backups) — root-anchored so this
# does NOT catch backend/app/data/ (seed content, must be committed)
/data/

# Downloaded ML models / voices / HF cache
/models/

# Misc
*.log
.DS_Store
Thumbs.db

# Claude Code session-local state (not project config)
.claude/settings.local.json
.claude/scheduled_tasks.lock
